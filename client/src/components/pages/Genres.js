const Genres = ({id, name}) => {
    // console.log(name);
    return(
        <>
        <div className="genresblk">
            <span>{name}</span>
        </div>
        </>
    )
}
export default Genres;